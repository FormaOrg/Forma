services:

  postgres:
    image: postgres:15-alpine
    container_name: forma_postgres
    environment:
      POSTGRES_DB: forma_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: hamma1212
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  service-utilisateurs:
    build: .
    container_name: forma_users
    ports:
      - "8081:8081"
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/forma_db
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: hamma1212
      JWT_SECRET: Zm9ybWEtc3VwZXItc2VjcmV0LWtleS1mb3ItZGV2ZWxvcG1lbnQtMjAyNQ==
      MAIL_USERNAME: mo.ba.hcn@gmail.com
      MAIL_PASSWORD: eltf dpqf ikzm ypex
      FRONTEND_URL: http://localhost:4200
    depends_on:
      - postgres
    volumes:
      - uploads_data:/app/uploads

volumes:
  postgres_data:
  uploads_data: